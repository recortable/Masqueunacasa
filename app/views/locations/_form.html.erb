<%= simple_form_for [parent, location] do |f| %>
  <div class="map" style="width: 300px; height: 300px;"></div>
  <p class="hint">
  Clica en el mapa para establecer una localización. Las localizaciones también necesitan un nombre.
  </p>
  <%= f.input :title %>
  <%= f.input :latitude %>
  <%= f.input :longitude %>
  <%= submit_button_for f %>
<% end %>
