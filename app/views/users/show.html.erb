<div class="row">
  <%= corner_span('info span4') do %> 
    <%= title user.name %>
    <%= content_tag(:p, 'Administrador(a) de masqueunacasa.org') if user.admin? %>
    <%= raw user.description %>
    <%= link_to 'Cerrar sesión', logout_path %>
    <%= link_to 'Editar', [:edit, user] if can? :edit, user %>
  <% end %>
</div>
