<% if document.respond_to?(:editorships) %>
  <div class="editorships">
    <p>
    En la elaboración de esta 
    <%= t("activerecord.models.#{document.class.name.downcase}") %>
    han participado:
    </p>

    <% document.editorships.each do |editorship| %>
      <%= avatar_link editorship.user, size: :small, class: 'fixed' %>
  <% end %>
  <br><br>
  <h5>Próximamente se habilitará la zona de comentarios / discusión</h5>
  </div>
<% end %>

