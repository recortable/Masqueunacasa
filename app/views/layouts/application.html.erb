<!DOCTYPE html>
<html class="<%= controller_class %> <%= themes %> <%= subdomain_class %>">
  <head> 
    <title><%= yield(:title) %> :: <%= current_group.name %></title>
    <link href='/favicon.png' rel='shortcut icon' type='image/png'>
    <!--[if lt IE 9]>
    <script src="http://html5shim.googlecode.com/svn/trunk/html5.js" type="text/javascript"></script>
    <![endif]-->
    <%= stylesheet_link_tag "application", :media => "all" %>
    <%= javascript_include_tag "application" %>
    <!-- TODO: Pau esto debería inyectarse -->
    <!--script type="text/javascript"
      src="http://maps.googleapis.com/maps/api/js?sensor=false&language=<%= I18n.locale %>&region=ES">
    </script--!>
    <%= csrf_meta_tags %>
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <%= yield :head %>
  </head>
  <body>
    <%= render 'session' %>
    <%= render 'flashes' %>

    <div id="main" class="container">
      <%= yield %>
    </div>
    <%= render 'layouts/ga' %>
  </body>
</html>
