  <%= link_to avatar_image(group, class: 'fixed'), profile_path %>
  <div class="members">
    <% group.members.limit(4).each do |user| %>
      <%= link_to avatar_image(user, size: :small, class: 'fixed'), user_url(user, host: request.domain) %>
    <% end %>
  </div>
  <%= render_group_sections group %>
