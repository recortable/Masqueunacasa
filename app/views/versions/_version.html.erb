<%= div_for version do %>
  <% if version.user_name.present? %>
    <%= link_to version.user_name, user_path(version.whodunnit) %>
  <% else %>
    Anónimx
  <% end %>

  ha <%= t("versions.events.#{version.event}") %>
<strong>
  la <%= t("activerecord.models.#{version.item_type.downcase}") %>
  </strong>
  <%= link_to_if version.event != 'destroy', version.title, version %>
  <br>
  Hace <%= time_ago_in_words version.created_at %>
<% if version.document.present? %>
  en <%= link_to version.parent_title, version.document %>
<% end %>
  <% end %>

