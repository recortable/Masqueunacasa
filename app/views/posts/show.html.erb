<%= page head: false, row: false do %>
  <%= render_group_header post.group %>

  <div class="row">
    <%= user_column 'span2' do %>
      <%= link_to avatar_image(post.group, class: 'fixed'), posts_path %> 
      <%= render_kudos post %>
      <% if post.group == current_group %>
        <%= render_group_sections %>
      <% end %>
    <% end %>
    <div class='span6 center post'>
      <div class="hoverize">
        <%= hover? :update, post do %>
          <%= link_to icon(:pencil, 'Editar'), [:edit, post] %>
          <%= link_to 'Editar', [:edit, post] %>
          <% if can? :delete, post %>
            |  <%= link_to icon(:trash), post, method: :delete,
              rel: "delete-post-#{post.id}", data: { confirm: '¿Quieres borrar esta entrada?' } %>
          <% end %>
        <% end %>
        <h3><%= l post.published_at %></h3>
        <%= title post.title %>
        <%= render_post_media(post) %>
      </div>
      <%= render_sections post %>
      <%= raw post.body %>
      <%= simple_list post.attachments do |attachment| %>
        <%= link_to attachment.file_identifier, attachment.file_url %>
      <% end %>
    </div>

    <div class="sidebar span4">
      <%= render_posts_archive post.group.posts, post.group %>
    </div>
  </div>
<% end %>
