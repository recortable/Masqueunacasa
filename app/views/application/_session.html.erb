<div id="session" class="navbar container">
  <div class="navbar-inner">
    <div class="container">
      <ul class="nav pull-left primary-links">
        <li><%= corner_link 'Habitapedia', phases_url(host: request.domain) %></li>
        <!-- <li><%# corner_link 'Mapa', locations_url(host: request.domain) %></li> -->
        <li><%= corner_link t('.community'), community_url(host: request.domain) %></li>
      </ul>
      <ul class="nav pull-right secondary-links">
        <li><%= corner_link "Sobre el proyecto", "#" %></li>
        <li><%= corner_link "Contacto", "#" %></li>
         <!-- TODO: implementar el cambio de idiomas 
         <div class="lang-selector">
           <%#= corner_link 'ES', url_for(locale: 'es') %>
           <%#= corner_link 'CA', url_for(locale: 'ca') %>
         </div>
         -->
      </ul>
      <ul class="nav pull-right session-links">
        <% if current_user %>
          <li><%= corner_link "¡Hola #{current_user.name}!", user_url(current_user, host: request.domain), class: 'user' %></li>
          <li><%= corner_link t('.session_end'), logout_url(host: request.domain, from: request.url) %></li>
        <% else %>
          <li><%= corner_link t('.session_start'), login_url(host: request.domain, from: request.url), class: 'login' %></li>
        <% end %>
      </ul>
      
    </div>
  </div>
</div>

