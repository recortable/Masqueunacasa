<fieldset class="locations" id="location">
  <legend><%= Location.model_name.human(count: 1).capitalize %></legend>
  <% form.object.build_location if form.object.location.blank? %>
  <%= form.fields_for :location do |f| %>
    <%= f.input :address %>
  <% end %>
</fieldset>
